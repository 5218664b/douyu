# Rebuild Plan

本文档用于落实树莓派 4B 优先的 `douyu-streamer` 重构执行方案。

与 [README.md](/home/x/code/gitsync/douyu/README.md) 的分工如下：

- `README.md`：描述目标架构与系统边界
- `REBUILD_PLAN.md`：描述阶段目标、执行 TODO、验收标准与范围控制

本文档默认前提：

- 新系统的主线是 `Go 主程序 + ffmpeg 直推斗鱼`
- 树莓派 4B 是首要部署环境
- `systemd` 是默认守护方式
- 自动取码不是主链路能力
- 本地 relay 不是默认架构组成部分

## 总体目标

重构完成后，项目应达到以下结果：

- 在树莓派 4B 上长期稳定运行
- 主系统只保留一套 Go 实现
- 宿主机依赖收敛到 `ffmpeg + systemd`
- 播放、切换、弹幕控制、状态输出由主程序统一承担
- 推流码通过配置注入，不依赖常驻浏览器自动化
- 旧脚本目录不再承担主入口职责

## 阶段划分

整个重构分为六个阶段：

1. 项目骨架阶段
2. 配置与运行时阶段
3. 媒体库与播放队列阶段
4. 推流主链路阶段
5. 弹幕控制与状态接口阶段
6. 交付收口阶段

## 第一阶段：项目骨架

### 阶段目标

建立适合树莓派轻量部署的新目录结构和基础工程形态，彻底脱离旧脚本式组织方式。

### 交付结果

- Go 工程骨架建立完成
- 主程序入口建立完成
- 核心模块目录建立完成
- `systemd` 配置目录建立完成
- 文档目录建立完成

### TODO

- 新建 `cmd/streamer/`
- 新建 `internal/config/`
- 新建 `internal/library/`
- 新建 `internal/playlist/`
- 新建 `internal/stream/`
- 新建 `internal/danmaku/`
- 新建 `internal/state/`
- 新建 `internal/api/`
- 新建 `configs/`
- 新建 `configs/systemd/`
- 新建 `scripts/preprocess/`
- 新建 `docs/`
- 新建 `testdata/`
- 初始化 Go module
- 建立主程序最小入口
- 建立基础编译方式说明

### 验收标准

- 新目录结构完整存在
- 主程序可编译为最小二进制
- 旧目录不再作为新主流程入口依赖

## 第二阶段：配置与运行时

### 阶段目标

建立统一配置模型和树莓派上的默认运行约束，使系统摆脱硬编码路径和零散脚本配置。

### 交付结果

- `app.yaml` 模型完成
- 环境变量覆盖规则完成
- 主程序配置加载模块完成
- `systemd` 服务模板完成
- 固定路径约定完成

### TODO

- 设计配置结构体
- 定义配置文件读取逻辑
- 定义环境变量覆盖机制
- 定义默认配置与校验规则
- 定义树莓派本地路径约定
- 定义视频目录位置约定
- 定义日志目录约定
- 定义配置缺失时的失败策略
- 定义 `systemd` service 文件模板
- 定义启动、重启和停止行为

### 验收标准

- 主程序启动不再依赖代码中的硬编码路径
- 关键配置项均可从文件或环境变量注入
- `systemd` 模板可作为标准守护方式
- 缺失关键配置时，系统能明确报错并退出

## 第三阶段：媒体库与播放队列

### 阶段目标

完成本地视频扫描、媒体项建模、播放队列与历史记录逻辑，为推流主链路提供稳定输入。

### 交付结果

- 视频目录扫描完成
- 媒体项模型完成
- 播放队列完成
- 当前项 / 下一项状态维护完成
- 重载与历史记录完成

### TODO

- 实现视频扫描器
- 识别支持的媒体扩展名
- 定义媒体项数据结构
- 实现媒体元数据读取
- 定义播放队列结构
- 实现顺序播放策略
- 实现最近播放历史结构
- 实现当前项和下一项查询
- 实现媒体库重载逻辑
- 实现空媒体库处理逻辑

### 验收标准

- 系统能读取本地视频目录中的素材
- 播放队列能稳定生成
- 媒体库重载不会破坏主状态
- 当前项、下一项和历史记录可被正确读取

## 第四阶段：推流主链路

### 阶段目标

完成 `ffmpeg` 进程托管、直推斗鱼、切换流程和错误恢复，形成树莓派上的最小可用主流程。

### 交付结果

- `ffmpeg` 启动逻辑完成
- `ffmpeg` 停止和重启逻辑完成
- 直推斗鱼链路完成
- 系统状态机完成
- 基础错误恢复完成

### TODO

- 设计 `ffmpeg` 命令生成器
- 实现推流参数注入
- 实现当前媒体推流启动逻辑
- 实现进程退出状态捕获
- 实现异常退出后的恢复逻辑
- 实现手动停止与重新启动逻辑
- 定义 `idle` / `preparing` / `streaming` / `switching` / `recovering` / `error`
- 实现顺序切换逻辑
- 实现下一项切换逻辑
- 实现主流程日志输出

### 验收标准

- 系统能把当前媒体推送到斗鱼 RTMP 地址
- 系统能按播放队列切换到下一项
- `ffmpeg` 异常退出时，主程序能进入正确状态
- 直推主链路可连续运行，不依赖 relay

## 第五阶段：弹幕控制与状态接口

### 阶段目标

把斗鱼弹幕控制和本地状态 API 接入主系统，但保持能力边界克制，不把系统推成复杂后台。

### 交付结果

- 斗鱼弹幕连接完成
- 小型命令集完成
- 本地 HTTP API 完成
- 状态输出完成

### TODO

- 实现斗鱼弹幕连接器
- 实现弹幕消息解码
- 实现 `#数字`
- 实现 `#next`
- 实现 `#reload`
- 实现命令校验与忽略策略
- 实现弹幕异常重连
- 实现本地 HTTP 路由
- 实现状态查询接口
- 实现播放队列查询接口
- 实现最近错误查询接口
- 实现手动重载和下一项触发接口

### 验收标准

- 系统可连接斗鱼弹幕通道
- 合法命令可影响播放队列
- 非法命令不会破坏主流程
- 本地 API 可读取当前状态和播放信息

## 第六阶段：交付收口

### 阶段目标

固化树莓派上的安装方式、预处理策略、日志说明和旧入口边界，使项目形成可持续维护的最小产品形态。

### 交付结果

- 配置样例完成
- `systemd` 使用说明完成
- 预处理说明完成
- 状态与日志说明完成
- 旧入口边界说明完成

### TODO

- 补齐 `app.yaml` 示例
- 补齐环境变量说明
- 补齐 `systemd` 安装与启用说明
- 补齐 `ffmpeg` 依赖说明
- 补齐视频预处理说明
- 补齐日志字段说明
- 补齐状态字段说明
- 补齐本地 API 文档
- 标记旧目录为历史实现
- 校对架构文档与执行文档一致性

### 验收标准

- 树莓派上的安装和启动方式完整可读
- 配置、状态和 API 有明确文档
- 旧目录不会再被误认为当前主入口
- 新旧文档边界清晰，没有互相冲突

## 优先级原则

整个重构过程遵循以下优先级：

1. 先固定目录和工程形态
2. 再固定配置和运行模型
3. 再完成媒体库和播放队列
4. 再打通 `ffmpeg` 直推主链路
5. 再接入弹幕和本地 API

这条顺序的含义是：

- 不能让弹幕逻辑反向主导主链路设计
- 不能让自动取码拖住主系统建设
- 不能为未来可能加入的 relay 预先污染当前结构

## 范围控制

以下内容不属于当前重构主线：

- 常驻浏览器自动化取码
- 多容器编排作为默认运行方式
- 本地 relay 中转作为默认链路
- 数据库存储
- 多用户后台
- 复杂 Web 控制台
- 多平台桌面体验统一

这些能力可以后续评估，但不允许进入当前主线目标。

## 风险点

当前重构最主要的风险有五类：

- 旧脚本逻辑直接搬运，导致新结构继续混乱
- 忽略树莓派 IO 和散热约束，导致方案纸面成立但实际不稳
- 过度追求无缝切换，提前把 relay 引回主链路
- 自动取码需求膨胀，拖累主系统稳定性
- 视频格式策略不收敛，导致运行期转码成本过高

因此执行中应坚持：

- 先做稳定直推，再谈更平滑切换
- 先做预处理友好的素材策略，再谈全格式兼容
- 先做 `systemd + Go + ffmpeg` 主线，再谈容器化补充

## 完成标志

当以下条件同时满足时，可以认为本次重构达到树莓派主线目标：

- 主程序已成为唯一主入口
- 系统已在树莓派 4B 上以 `systemd` 方式稳定运行
- `ffmpeg` 直推斗鱼已形成稳定主链路
- 弹幕命令已能有限度控制播放队列
- 推流码不再依赖常驻浏览器自动化
- 旧脚本目录不再承担生产意义上的主入口职责
